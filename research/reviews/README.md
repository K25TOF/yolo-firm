# Session Reviews

PO review documents generated after each research session.

Files are named `YYYY-MM-DD-{session-id}-review.md` and contain the Manager's closing synthesis.

These files are gitignored (operational output, not versioned).
